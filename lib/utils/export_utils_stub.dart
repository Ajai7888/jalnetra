// lib/utils/export_utils_stub.dart

import 'dart:async';

Future<void> saveAndShareCsv({
  required String filename,
  required String csvContent,
  required String shareText,
}) async {
  throw UnsupportedError('CSV export not supported on this platform');
}
